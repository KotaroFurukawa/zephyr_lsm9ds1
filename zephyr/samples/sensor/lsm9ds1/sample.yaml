sample:
  name: LSM9DS1 Sensor sample
tests:
  sample.sensor.lsm9ds1:
    harness: console
    tags: sensors
    depends_on: i2c lsm9ds1
    harness_config:
        type: one_line
        regex:
            - "temp: (.*); press: (.*); humidity: (.*)"
        fixture: fixture_i2c_bme280
  sample.sensor.bme280.spi:
    harness: console
    tags: sensors
    depends_on: spi bme280
    extra_args: "CONF_FILE=prj_spi.conf"
    harness_config:
        type: one_line
        regex:
            - "temp: (.*); press: (.*); humidity: (.*)"
        fixture: fixture_spi_bme280
